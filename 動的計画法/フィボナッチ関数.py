a = [1] * (n + 1) # n個の1次元配列を用意

for i in range(3, n + 1):   # 3からnまでの範囲でループ
    a[i] = a[i - 1] + a[i - 2]  # a[i-1]とa[i-2]の和をa[i]に代入
    # フィボナッチ数列の計算

k = int(input())    # フィボナッチ数列のk番目の値を取得
print(a[k])